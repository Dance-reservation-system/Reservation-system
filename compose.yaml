services:
  kamann_db:
    container_name: kamann_db
    build:
      context: .
      dockerfile: postgres.dockerfile
    environment:
      POSTGRES_DB: kamann_db
      POSTGRES_USER: user
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - postgres_db:/var/lib/postgres/data

volumes:
  postgres_db: